name: churn_analytics
version: "1.0"
config-version: 2

profile: churn_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  churn_analytics:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts
